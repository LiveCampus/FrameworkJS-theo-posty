{
  "name": "personal-template",
  "version": "1.0.0",
  "description": "",
  "dependencies": {
    "pnpm": "^7.22.0"
  },
  "devDependencies": {
    "turbo": "^1.6.3",
    "husky": "^8.0.3",
    "lint-staged": "^13.1.0"
  },
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "gen-types": "turbo run gen-types"
  }
}
